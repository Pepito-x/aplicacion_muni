name: muni_incidencias
description: "Aplicación Flutter para reportar incidencias en la Municipalidad"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- Firebase (Versiones estables) ---
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.2
  firebase_messaging: ^15.1.3

  # --- PDF y archivos ---
  pdf: ^3.10.4
  open_filex: ^4.5.0
  path_provider: ^2.1.4
  excel: ^4.0.3
  shimmer: ^3.0.0
  record: ^5.1.0          # Para grabar audio
  audioplayers: ^6.0.0    # Para reproducir audio
  permission_handler: ^11.3.0 # Para permisos de micrófono

  # --- Gráficos ---
  syncfusion_flutter_charts: ^26.2.9

  # --- UI y utilidades ---
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  qr_flutter: ^4.1.0
  uuid: ^4.5.1
  mobile_scanner: ^5.2.1
  http: ^1.2.2
  cached_network_image: ^3.4.1
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2
  flutter_svg: ^2.0.10
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.7.0
  intl: ^0.19.0
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/img/
    - assets/fonts/

  fonts:
    - family: Montserrat
      fonts:
        - asset: assets/fonts/Montserrat-Regular.ttf
        - asset: assets/fonts/Montserrat-Medium.ttf
          weight: 500
        - asset: assets/fonts/Montserrat-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Montserrat-Bold.ttf
          weight: 700

# 👇 CONFIGURACIÓN DEL ÍCONO
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/img/logo_reque.png"
  min_sdk_android: 21

# 👇👇👇 AGREGA ESTO AL FINAL DEL ARCHIVO PARA CORREGIR EL ERROR 👇👇👇
dependency_overrides:
  record_linux: ^1.1.0
  record_platform_interface: ^1.4.0